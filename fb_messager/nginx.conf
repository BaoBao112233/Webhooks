# FB & Messenger Webhooks - Nginx Configuration
# Place this file in /etc/nginx/sites-available/fb-webhooks
# Then create a symlink: sudo ln -s /etc/nginx/sites-available/fb-webhooks /etc/nginx/sites-enabled/

upstream fb_webhooks {
    server 127.0.0.1:3001;
    keepalive 64;
}

server {
    listen 80;
    server_name webhooks.yourdomain.com www.webhooks.yourdomain.com;

    # Redirect HTTP to HTTPS (uncomment after setting up SSL)
    # return 301 https://$server_name$request_uri;

    # For testing without SSL
    location / {
        proxy_pass http://fb_webhooks;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Increase timeout for webhook processing
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Logging
    access_log /var/log/nginx/fb-webhooks-access.log;
    error_log /var/log/nginx/fb-webhooks-error.log;
}

# HTTPS Configuration (use after setting up SSL with Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name webhooks.yourdomain.com www.webhooks.yourdomain.com;
#
#     # SSL Certificate Configuration
#     ssl_certificate /etc/letsencrypt/live/webhooks.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/webhooks.yourdomain.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://fb_webhooks;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     access_log /var/log/nginx/fb-webhooks-access.log;
#     error_log /var/log/nginx/fb-webhooks-error.log;
# }
